# Workbench design assets

`design_workbench_v1.design.json` is the editable design model, and
`workbench.html` is its visual reference. The current application is
`apps/ImagingWorkbench`; these assets are documentation, not a build package.
