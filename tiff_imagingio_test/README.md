# tiff_imagingio_test

Focused first-slice ImagingIO TIFF contract. It covers exact transactional ZIP roundtrips for UInt8 RGB/RGBA, UInt16 GrayAlpha and Float32 Gray; straight-alpha preservation; source immutability; explicit Float16 and non-zero-origin refusal; malformed-load output preservation; transaction residue and cleanup.

Expected summary: `passed=29 failed=0`.
