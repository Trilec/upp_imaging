# ocio_dependencies_test

Aggregate OpenColorIO dependency coexistence test.

It exercises Expat, yaml-cpp, pystring, minizip-ng, Imath, and zlib in one
executable to catch duplicate symbols and include-path drift early.
