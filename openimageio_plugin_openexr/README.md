# openimageio_plugin_openexr

Statically registers the pinned OpenImageIO 3.1.15.0 OpenEXR plugin sources
with OpenImageIO. The package exposes the OIIO creator/extension registration
without dynamic plugin lookup. OpenEXR include compatibility headers are owned
by the stable `openexr/OpenEXR` surface.
