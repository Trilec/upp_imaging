# jpegxl_prereq_test

Focused GUI-free contract test for the pinned libjxl source backend. It checks the
0.12.0 runtime API, resizable thread runner, deterministic lossless RGB8 encode/decode,
basic image information, exact decoded bytes, signature recognition and malformed-input rejection.

Expected summary: `passed=9 failed=0`.
