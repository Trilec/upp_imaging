# libdeflate

`libdeflate` is the user-facing libdeflate package for U++ applications.

## Include style

```cpp
#include <libdeflate/libdeflate.h>
```

## Current behavior

For this first pass, `libdeflate` delegates directly to `libdeflate_src`.
