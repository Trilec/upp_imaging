#ifndef UPP_OPENCOLORIO_SRC_LOGGING_FORWARDER_H
#define UPP_OPENCOLORIO_SRC_LOGGING_FORWARDER_H

// OpenColorIO apphelpers include the private logging API as "Logging.h".
// Keep that generic name deterministic inside the U++ aggregate include graph.
#include "upstream/src/OpenColorIO/Logging.h"

#endif
