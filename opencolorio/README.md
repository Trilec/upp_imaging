OpenColorIO user-facing wrapper package.

Stable include path: `#include <opencolorio/OpenColorIO.h>`.
Forwards to the canonical OpenColorIO public headers from `opencolorio_src`.

Static CLANGx64 package; no renderer, bindings, or tools.
