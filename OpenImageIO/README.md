# openimageio

Stable application package for OpenImageIO. Include
`<openimageio/OpenImageIO.h>` and use `UppImaging::LoadImage` or
`UppImaging::SaveImage` for statically registered EXR and PNG support.
