description "Pinned LibRaw 0.22.2 source backend for U++";

noblitz;

include
	upstream;

options
	-DLIBRAW_NODLL=1,
	-DLIBRAW_BUILDLIB=1,
	-D_USE_MATH_DEFINES=1;

library(WIN32) ws2_32;

file
	import.ext,
	README.md;
