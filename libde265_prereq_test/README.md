# libde265_prereq_test

Focused scalar HEVC decoder contract for repository-pinned libde265 1.1.1.

The test verifies string/numeric version identity, explicit static initialization/shutdown, scalar decoder construction, deterministic no-input state and clean context destruction.

Expected summary: `passed=7 failed=0`.
