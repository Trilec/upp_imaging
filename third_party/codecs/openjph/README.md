# openjph

`openjph` is the user-facing OpenJPH package for U++ applications.

## Include style

```cpp
#include <openjph/openjph.h>
```

## Current behavior

For this first pass, `openjph` delegates directly to `openjph_src`.
